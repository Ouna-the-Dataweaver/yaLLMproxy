# yaLLMproxy
Yet another LLM proxy/router. The focus was on simplicity and lack of any possible errors, so in the base configuration it just acts as a bypass proxy with no change to the payload and by design can't introduce new errors (unlike LiteLLM)
