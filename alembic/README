# Database Migrations

This directory contains Alembic migrations for the yaLLMproxy database.

## Usage

### Run migrations
```bash
uv run alembic upgrade head
```

### Create a new migration
```bash
uv run alembic revision --autogenerate -m "your migration description"
```

### Rollback last migration
```bash
uv run alembic downgrade -1
```

### Check current revision
```bash
uv run alembic current
```

## Configuration

The migration environment is configured in `env.py` and reads database settings from `configs/config_default.yaml`.
